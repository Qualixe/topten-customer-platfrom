"""Customers module: read-only customer listing for the dashboard.

Only a list endpoint exists so far (GET /api/v1/customers) — profile editing,
VIP calculation, and birthday automation are future phases; see the README's
"Recommended Phase 3".
"""
